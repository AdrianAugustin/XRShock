<Window
    x:Class="ShockCollar.View.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ShockCollar"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="clr-namespace:ShockCollar.ViewModels"
    xmlns:uc="clr-namespace:ShockCollar.UserControls"
    x:Name="mainWindow"
    Title="XRShock"
    Width="800"
    Height="450"
    mc:Ignorable="d"
    Closing="Window_Closing"
    ShowInTaskbar="{Binding ShowInTaskbar}"
    WindowState="{Binding WindowState}"
      >



   




        <!--<Window.Resources>
        <DataTemplate x:Name="EntityConfigDataTemplate" DataType="{x:Type viewmodels:ViewModel}">

        </DataTemplate>
    </Window.Resources>-->

    <!--<Window.Resources>
        <DataTemplate x:Name="EntityConfigDataTemplate" DataType="{x:Type ViewModel}">
            <View:EntityConfigOverview DataContext="{Binding UpdateSourceTrigger=PropertyChanged}" />
        </DataTemplate>
    </Window.Resources>-->



    <Grid Background="#FF6F6F6F">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0" />
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
            
            <ColumnDefinition Width="0" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="0" />

            <RowDefinition Height="*" />
            <RowDefinition Height="*" />



            <RowDefinition Height="0" />
        </Grid.RowDefinitions>
        <uc:ModeSelector Grid.Column="1" Grid.Row="1" Grid.ColumnSpan="4"></uc:ModeSelector>
        <uc:ControlSliders Grid.Row="2" Grid.Column="1"  Grid.ColumnSpan="2" VerticalAlignment="Center" ></uc:ControlSliders>
        <uc:ArduinoApiGui Grid.Column="4" Grid.Row="2"></uc:ArduinoApiGui>
        <uc:OSCApiGui Grid.Column="3" Grid.Row="2"></uc:OSCApiGui>

        <!--<GridSplitter Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="6" Height="20" HorizontalAlignment="Stretch"
></GridSplitter>

        <Button
            Grid.Row="4"
            Grid.Column="3"
            Margin="0,0
            ,0,0"
            HorizontalAlignment="Center"
            VerticalAlignment="Top"
            Content="Activate" />-->
     

    </Grid>
</Window>
